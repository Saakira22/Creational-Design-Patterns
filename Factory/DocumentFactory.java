public interface DocumentFactory {

//The DocumentFactory interface defines the factory method (createDocument) that is responsible for creating instances of the Document interface.

 Document createDocument();
}
